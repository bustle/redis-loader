{
  "name": "redis-loader",
  "version": "4.0.0",
  "description": "An io-redis like object that batches queries with dataloader",
  "author": "Tristan Siegel <tristan.siegel@bustle.com>",
  "license": "MIT",
  "engines": {
    "node": ">=6"
  },
  "main": "dist",
  "scripts": {
    "build": "yarn clean && tsc",
    "clean": "rm -rf dist/*",
    "docs": "rm -rf ./docs && typedoc --out ./docs/ --theme minimal src/*.ts && touch ./docs/.nojekyll",
    "lint": "eslint --ignore-path .gitignore src/**/*.ts && tsc",
    "format": "eslint --ignore-path .gitignore src/**/*.ts test/**/*.ts --fix",
    "prepublish": "yarn build",
    "test": "jest --runInBand --bail --coverage"
  },
  "dependencies": {
    "bluestream": "10",
    "dataloader": "^1.4.0",
    "invariant": "^2.2.4",
    "redis-commands": "^1.3.5"
  },
  "peerDependencies": {
    "ioredis": "^3.0.0"
  },
  "devDependencies": {
    "@types/ioredis": "^3.2.14",
    "@types/jest": "^23.3.1",
    "@types/node": "^10.5.7",
    "bluebird": "^3.5.1",
    "chai": "^4.1.2",
    "eslint": "^5.3.0",
    "eslint-config-standard": "^11.0.0",
    "eslint-plugin-import": "^2.13.0",
    "eslint-plugin-jest": "^21.20.2",
    "eslint-plugin-node": "^7.0.1",
    "eslint-plugin-promise": "^3.8.0",
    "eslint-plugin-standard": "^3.1.0",
    "eslint-plugin-typescript": "^0.12.0",
    "ioredis": "^3.0.0",
    "jest": "^23.5.0",
    "ts-jest": "^23.1.3",
    "ts-node": "^7.0.0",
    "tslint": "^5.11.0",
    "typedoc": "^0.11.1",
    "typescript": "^3.0.1",
    "typescript-eslint-parser": "^18.0.0"
  }
}
